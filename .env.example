# Copie para .env.local e preencha. .env.local não vai pro git.

# Destino dos leads do formulário de diagnóstico.
# Webhook do n8n que recebe o POST em JSON.
# Sem esta variável o formulário FALHA de propósito (mostra erro na tela em vez
# de fingir sucesso) — ver client/src/lib/submitLead.ts
VITE_LEAD_WEBHOOK_URL=
